# VectorShift Pipeline Builder

## Frontend
- React + React Flow + Zustand
- Start with: `cd project_frontend && npm start`

## Backend
- FastAPI
- Start with: `cd project_backend && uvicorn main:app --reload --port 8000`
